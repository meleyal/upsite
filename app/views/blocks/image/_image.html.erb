<div class="block block-image col-sm-4">
  <div class="block-inner">
    <% if image.url.present? %>
      <a href="<%= image.url %>">
    <% end %>
      <img src="<%= image.image %>">
    <% if image.url.present? %>
      </a>
    <% end %>
  </div>
</div>
