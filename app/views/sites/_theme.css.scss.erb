<% if @site.color.present? %>
  <style>
    html {
      background: <%= @site.border.present? ? @site.color : '#fff' %>;
    }
    .block-text a,
    .block-text a:hover {
      color: <%= @site.color %>;
    }
  </style>
<% end %>
