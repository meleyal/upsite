<header class="page-header">
  <h1>
    <%= @site.title %>
    <small><%= @site.subdomain %>.sitespot.com</small>
  </h1>
</header>

<div class="widgets row"></div>

<footer class="page-footer">
  &copy; <%= "#{Date.today.year} #{@site.title}" %>
</footer>

<% content_for :bootstrap_data do %>
  <!-- app.widgets.update(<%= render(template: 'widgets/index.json').html_safe %>); -->
  app.site.set(<%= json_escape @site.to_json.html_safe %>);
<% end %>
