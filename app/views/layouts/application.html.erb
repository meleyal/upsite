<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="generator" content="upsite.io" />
  <%= csrf_meta_tags %>

  <title><%= content_for?(:title) ? content_for(:title) : t('tagline') %> | <%= t('app_name') %></title>

  <!-- Favicons -->
  <link rel="shortcut icon" href="<%= content_for(:favicon) %>">

  <!-- Open Graph Tags -->
  <meta property="og:url" content="<%= content_for(:url) %>">
  <meta property="og:type" content="website">
  <meta property="og:title" content="<%= content_for(:title) %> | <%= t('app_name') %>">
  <meta property="og:description" content="<%= content_for(:description) %>">
  <meta property="fb:app_id" content="221451268244315">

  <!-- Assets -->
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= yield :stylesheets %>
</head>

<body>

  <div class="container">
    <div class="alert alert-info alert-banner">
      Upsite is shutting down on 1 June 2017 &mdash; <a href="mailto:help@upsite.io">Contact us</a> if you need assistance.
    </div>
    <%= yield %>
  </div>

  <!-- Analytics -->
  <%= render 'google' %>
  <%= yield :javascripts %>

</body>

</html>
