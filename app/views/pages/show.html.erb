<% content_for :page_title, @page.name %>

<%= render 'theme.scss' %>

<% if false && @blocks.present? %>
  <%= render @blocks %>
<% else %>
  <%= render 'blank_slate' %>
<% end %>

<%= render 'hud' %>
<%= render 'modal' %>
<%= render 'modal', size: 'small' %>

<!-- <a class="app-link" href="http://www.pagekitapp.com/" target="_blank" title="<%= t('tagline') %>"><%= t('app_name') %></a> -->
