<% content_for :page_title, @page.name %>

<%= render 'theme.scss' %>

<div class="row">
  <%= render @widgets %> 
  <%# render 'widgets/footer/footer' %>
</div>

<%= render 'hud' %>
<%= render 'modal' %>
<%= render 'modal', size: 'small' %>
