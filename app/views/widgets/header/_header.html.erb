<header class="page-header">
  <div class="container">
  <h1><%= header.title %></h1>


  <%# debug header.attachments %>

  <% header.attachments.each do |attachment| %>
    <%= image_tag attachment.upload.url %>
    <%= debug attachment.upload.url %>
  <% end %>
  </div>
</header>

