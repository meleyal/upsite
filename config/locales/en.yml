en:
  app_name: Upsite
  tagline: A simple, free way to make a website for anything.
  tagline_short: Websites made simple

  signups:
    new:
      submit: Be the first to try it...

  activerecord:
      # models:
      #   user: Dude
      # attributes:
      #   user:
      #     first_name: "Name first"

  helpers:
    submit:
      create: Create
      update: Update
      site:
        create: Create your site
      session:
        submit: Login
      help_message:
        submit: Send
        # submit: "Save %{model}"
